[package]
name = "shared"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"

[lib]
name = "shared"
crate-type = ["staticlib", "cdylib", "rlib"]

[target.'cfg(target_os = "android")'.dependencies]
jni = "0.21"

